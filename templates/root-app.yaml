apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infrastructure-root
  namespace: {{ .Values.argocd.namespace | default "argocd" }}
  labels:
    app.kubernetes.io/name: infrastructure-root
    app.kubernetes.io/part-of: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: infrastructure
  source:
    repoURL: {{ .Values.spec.source.repoURL | default "https://github.com/Fizic/infrastructure-gitops.git" }}
    targetRevision: HEAD
    path: apps
  destination:
    server: {{ .Values.argocd.destination.server | default "https://kubernetes.default.svc" }}
    namespace: {{ .Values.argocd.namespace | default "argocd" }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagation=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
